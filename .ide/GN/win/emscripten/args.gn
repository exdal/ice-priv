is_clang = true
use_lld = true

# Faster compilation for a lot of files
enable_unity_build = true

# TODO: make sure we don't require this anymore
clang_base_path = rebase_path("//third-party/emsdk/upstream")

target_os = "emscripten"
target_cpu = "wasm32"
